<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bubblejet.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bit_feather_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bit_feather_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ChooseBird" />
        <activity android:name=".EndScreen" />
        <activity android:name=".HomeScreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".SettingsScreen" />
    </application>

</manifest>